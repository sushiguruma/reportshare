<% form_for(@reference) do |f| %>
  <div class="form-group">
    <%= f.label :content, '参考文献' %>
    <%= f.text_area :content, class: 'form-group', row: 3 %>
  </div>
  
  <%= f.submit '登録', class: 'btn btn-primary btn-block' %>
<% end %>